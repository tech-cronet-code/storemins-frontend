// import { createContext, ReactNode } from "react";

// interface CustomerContextContextType {
 
// }

// // 1. createContext
// export const CustomerContextContext =
//   createContext<CustomerContextContextType | null>(null);

// // 2. Provider component
// export const CustomerProvider = ({
//   children,
// }: {
//   children: ReactNode;
// }) => {

//   const contextValue = {
   
//   };

//   return (
//     <CustomerContextContext.Provider value={contextValue}>
//       {children}
//     </CustomerContextContext.Provider>
//   );
// };
